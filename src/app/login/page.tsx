import { DemoLoginModal } from "@/components/demo-login-modal";

export default function LoginPage() {
  return <main className="grid min-h-[60vh] place-items-center bg-slate-50 p-4"><DemoLoginModal initiallyOpen standalone /></main>;
}
